Create Django Project

pip install --user virtualenv

python -m virtualenv env
cd env
cd Scripts
activate
cd..
cd..

pip install Django

django-admin startproject JazzminProject
cd JazzminProject

pip install django
pip install django-jazzmin

python manage.py startapp core

Register App and Jazzmin in settings.py
Open myproject/settings.py and update:

INSTALLED_APPS = [
    'jazzmin',         # must come before 'django.contrib.admin'
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'core',            # your custom app
]


Optional: Customize Jazzmin
You can define custom styles, icons, and dashboard layout in settings.py:

JAZZMIN_SETTINGS = {
    "site_title": "My Project Admin",
    "site_header": "My Admin Dashboard",
    "welcome_sign": "Welcome to My Admin",
    "copyright": "My Company",
    "search_model": "auth.User",
    "show_sidebar": True,
    "navigation_expanded": True,
    "icons": {
        "auth.User": "fas fa-user",
        "auth.Group": "fas fa-users",
    }
}

Add Models in core/models.py

from django.db import models

class Category(models.Model):
    name = models.CharField(max_length=100)

    def __str__(self):
        return self.name

class Product(models.Model):
    category = models.ForeignKey(Category, on_delete=models.CASCADE)
    name = models.CharField(max_length=200)
    price = models.DecimalField(max_digits=10, decimal_places=2)
    stock = models.PositiveIntegerField(default=0)

    def __str__(self):
        return self.name

python manage.py makemigrations
python manage.py migrate
python manage.py createsuperuser


Register Models in core/admin.py

from django.contrib import admin
from .models import Category, Product

@admin.register(Category)
class CategoryAdmin(admin.ModelAdmin):
    list_display = ('id', 'name')

@admin.register(Product)
class ProductAdmin(admin.ModelAdmin):
    list_display = ('id', 'name', 'category', 'price', 'stock')
    list_filter = ('category',)
    search_fields = ('name',)


python manage.py runserver


Optional: Add Icons in settings.py

JAZZMIN_SETTINGS = {
    "site_title": "My Store Admin",
    "site_header": "My Store Dashboard",
    "welcome_sign": "Welcome to My Product Store",
    "icons": {
        "core.Category": "fas fa-tags",
        "core.Product": "fas fa-boxes",
    },
}


FontAwesome Icons: https://fontawesome.com/icons
